# 🚀 Быстрый старт: Внешние источники данных

## ⚡ За 5 минут

### 1. Установите зависимости

```bash
cd python
pip install -r requirements.txt
```

### 2. Запустите Redis (опционально, но рекомендуется)

**⚠️ Redis НЕ обязателен!** Приложение работает без него, просто без кэширования.

**Если хотите использовать Redis:**

**Вариант A: Docker Desktop (если установлен)**
```bash
docker run -d -p 6379:6379 --name redis redis:latest
```

**Вариант B: Без Redis (самый простой)**
- Просто пропустите этот шаг! Приложение будет работать без кэширования.

**Подробнее:** См. `REDIS_INSTALLATION_WINDOWS.md` для всех вариантов установки Redis.

### 3. Настройте .env (опционально)

Создайте/обновите `.env` файл:
```env
REDIS_HOST=localhost
REDIS_PORT=6379
CACHE_TTL=3600
```

### 4. Протестируйте провайдеры

```bash
python test_providers.py
```

### 5. Запустите FastAPI

```bash
uvicorn main:app --reload
```

### 6. Протестируйте API

```bash
# Поиск товаров
curl "http://localhost:8000/products?search=iPhone%2015"

# Получение товара
curl "http://localhost:8000/products/Apple/iPhone%2015"
```

---

## 📝 Основные эндпоинты

### Поиск товаров
```
GET /products?search=запрос&limit=20&shops=wildberries,dns
```

### Получение товара по бренду и модели
```
GET /products/{brand}/{model}
```

### Статистика кэша
```
GET /external/cache/stats
```

---

## 🔧 Что дальше?

1. **Прочитайте** `EXTERNAL_DATA_ONLY.md` для детальной информации
2. **Адаптируйте парсеры** под актуальную структуру сайтов
3. **Настройте мониторинг** и логирование
4. **Добавьте больше магазинов** при необходимости

---

## ❓ Проблемы?

- **Redis не запускается?** - Приложение работает без него, но без кэша
- **Парсеры не находят товары?** - Обновите селекторы в `parsers.py`
- **Медленные запросы?** - Используйте кэширование (`use_cache=true`)

---

*Для подробной информации см. `EXTERNAL_DATA_ONLY.md`*
