# 🔐 JWT (JSON Web Token) - Объяснение и реализация

## 📚 Что такое JWT?

**JWT (JSON Web Token)** — это стандартный способ безопасной передачи информации между сторонами в виде JSON-объекта. Это **токен доступа**, который позволяет пользователю работать с защищенными ресурсами без необходимости каждый раз вводить логин и пароль.

### 🎯 Зачем нужен JWT?

**Проблема без JWT:**
- Пользователь должен вводить логин/пароль при каждом запросе
- Пароли передаются по сети (небезопасно)
- Нужно хранить сессии на сервере

**Решение с JWT:**
- ✅ Пользователь логинится один раз
- ✅ Получает токен, который действует определенное время
- ✅ Токен автоматически добавляется в каждый запрос
- ✅ Сервер проверяет токен без обращения к базе данных
- ✅ Безопасно и масштабируемо

---

## 🔑 Как работает JWT?

### Структура JWT токена

JWT состоит из **трех частей**, разделенных точками:

```
header.payload.signature
```

**Пример:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsImV4cCI6MTY5ODc2NDgwMH0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c
```

#### 1. **Header (Заголовок)**
```json
{
  "alg": "HS256",  // Алгоритм шифрования
  "typ": "JWT"     // Тип токена
}
```

#### 2. **Payload (Полезная нагрузка)**
```json
{
  "sub": 1,                    // ID пользователя (subject)
  "exp": 1698764800,           // Время истечения (expiration)
  "iat": 1698763000            // Время создания (issued at)
}
```

#### 3. **Signature (Подпись)**
```
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  SECRET_KEY
)
```

**Подпись гарантирует:**
- Токен не был изменен
- Токен выдан именно вашим сервером
- Целостность данных

---

## 🔄 Процесс работы JWT в вашем проекте

### Шаг 1: Пользователь логинится

```
Пользователь → Android App → Backend API
```

**Android (`Auth.kt`):**
```kotlin
// Пользователь вводит логин и пароль
val loginRequest = LoginRequest(login, password)
RetrofitClient.apiService.loginUser(loginRequest)
```

**Backend (`main.py`):**
```python
@app.post("/login")
def come_in(user: schemas.UserLogin, db: Session = Depends(get_db)):
    # 1. Проверяем пользователя в БД
    db_user = db.query(User).filter(User.login == user.login).first()
    
    # 2. Проверяем пароль
    if not verify_password(user.password, db_user.password):
        raise HTTPException(401, "Неверный пароль")
    
    # 3. Создаем JWT токен
    access_token = create_access_token(
        data={"sub": db_user.id_user},  # ID пользователя в токене
        expires_delta=timedelta(minutes=30)  # Токен действует 30 минут
    )
    
    # 4. Возвращаем токен
    return {
        "access_token": access_token,
        "token_type": "bearer",
        "user": {...}
    }
```

**Функция создания токена (`main.py:92-100`):**
```python
def create_access_token(data: dict, expires_delta: Optional[timedelta] = None):
    to_encode = data.copy()
    
    # Добавляем время истечения
    if expires_delta:
        expire = datetime.utcnow() + expires_delta
    else:
        expire = datetime.utcnow() + timedelta(minutes=30)
    
    to_encode.update({"exp": expire})
    
    # Кодируем токен с секретным ключом
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
    return encoded_jwt
```

---

### Шаг 2: Сохранение токена на Android

**Android (`Auth.kt:86-90`):**
```kotlin
// После успешного логина
authManager.saveAuthData(
    loginResponse.access_token,  // Сохраняем токен
    loginResponse.user           // Сохраняем данные пользователя
)
```

**AuthManager (`AuthManager.kt:37-44`):**
```kotlin
fun saveAuthData(token: String, user: UserResponse) {
    prefs.edit().apply {
        putString(KEY_ACCESS_TOKEN, token)  // Сохраняем в SharedPreferences
        putInt(KEY_USER_ID, user.id_user)
        putString(KEY_USER_LOGIN, user.login)
        putString(KEY_USER_EMAIL, user.email)
        apply()
    }
}
```

**Где хранится:** `SharedPreferences` (локальное хранилище Android)

---

### Шаг 3: Автоматическое добавление токена в запросы

**RetrofitClient (`RetrofitClient.kt:41-57`):**
```kotlin
private val okHttpClient = OkHttpClient.Builder()
    .addInterceptor { chain ->
        val originalRequest = chain.request()
        
        // Получаем токен из AuthManager
        val token = authManager?.getToken()
        
        val requestBuilder = originalRequest.newBuilder()
            .addHeader("Content-Type", "application/json")
        
        // ⭐ ВАЖНО: Добавляем токен в заголовок Authorization
        if (token != null) {
            requestBuilder.addHeader("Authorization", "Bearer $token")
        }
        
        val request = requestBuilder.build()
        chain.proceed(request)  // Отправляем запрос с токеном
    }
    .build()
```

**Как это работает:**
- Каждый HTTP-запрос проходит через этот Interceptor
- Токен автоматически добавляется в заголовок `Authorization: Bearer <token>`
- Не нужно вручную добавлять токен в каждый запрос!

**Пример HTTP-запроса:**
```
GET /products HTTP/1.1
Host: 172.20.10.2:8000
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
```

---

### Шаг 4: Проверка токена на сервере

**Backend (`main.py:114-124`):**
```python
def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)):
    """
    Проверяет токен и возвращает текущего пользователя
    Используется как зависимость для защищенных эндпоинтов
    """
    credentials_exception = HTTPException(
        status_code=401,
        detail="Не удалось подтвердить учетные данные"
    )
    
    # 1. Проверяем и декодируем токен
    user_id = verify_token(token, credentials_exception)
    
    # 2. Получаем пользователя из БД
    user = db.query(User).filter(User.id_user == user_id).first()
    
    if user is None:
        raise credentials_exception
    
    return user  # Возвращаем пользователя
```

**Функция проверки токена (`main.py:103-111`):**
```python
def verify_token(token: str, credentials_exception):
    try:
        # Декодируем токен с секретным ключом
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        
        # Извлекаем ID пользователя
        user_id: int = payload.get("sub")
        
        if user_id is None:
            raise credentials_exception
        
        return user_id
    except JWTError:
        # Токен невалидный (истек, подделан, и т.д.)
        raise credentials_exception
```

**Что проверяется:**
- ✅ Подпись токена (не был ли изменен)
- ✅ Время истечения (`exp`)
- ✅ Формат токена
- ✅ Секретный ключ совпадает

---

### Шаг 5: Использование в защищенных эндпоинтах

**Пример защищенного эндпоинта:**
```python
@app.get("/favorites")
def get_favorites(
    current_user: User = Depends(get_current_user),  # ⭐ Проверка токена
    db: Session = Depends(get_db)
):
    # Если токен валидный, current_user содержит данные пользователя
    # Если токен невалидный, вернется ошибка 401
    
    favorites = db.query(Favorite).filter(
        Favorite.user_id == current_user.id_user
    ).all()
    
    return favorites
```

**Как это работает:**
1. Клиент отправляет запрос с токеном в заголовке
2. FastAPI автоматически извлекает токен из заголовка `Authorization`
3. Вызывается `get_current_user(token)`
4. Токен проверяется и декодируется
5. Пользователь получается из БД
6. Если все ОК, эндпоинт выполняется
7. Если токен невалидный, возвращается ошибка 401

---

## 🔒 Безопасность JWT

### ✅ Преимущества

1. **Stateless (без состояния)**
   - Серверу не нужно хранить сессии
   - Легко масштабировать (несколько серверов)

2. **Безопасность**
   - Токен подписан секретным ключом
   - Нельзя подделать без знания ключа
   - Время жизни ограничено

3. **Удобство**
   - Токен хранится на клиенте
   - Автоматически добавляется в запросы
   - Не нужно логиниться каждый раз

### ⚠️ Важные моменты безопасности

1. **Секретный ключ (`SECRET_KEY`)**
   ```python
   # ❌ ПЛОХО: Слабый ключ
   SECRET_KEY = "secret"
   
   # ✅ ХОРОШО: Длинный случайный ключ
   SECRET_KEY = os.getenv("SECRET_KEY", "your-secret-key-change-this-in-production-min-32-chars")
   ```
   - Должен быть длинным (минимум 32 символа)
   - Должен быть случайным
   - Хранится в `.env` (не в коде!)

2. **Время жизни токена**
   ```python
   # Токен действует 30 минут
   ACCESS_TOKEN_EXPIRE_MINUTES = 30
   ```
   - Не слишком долго (безопасность)
   - Не слишком коротко (удобство)

3. **HTTPS в продакшене**
   - Токены передаются по сети
   - Нужно использовать HTTPS (не HTTP)
   - Защищает от перехвата токенов

4. **Хранение на клиенте**
   - Android: `SharedPreferences` (зашифрованные)
   - Не хранить в логах
   - Не передавать в URL

---

## 📊 Схема работы JWT в вашем проекте

```
┌─────────────┐
│   Android   │
│   App       │
└──────┬──────┘
       │
       │ 1. POST /login (login, password)
       ▼
┌─────────────┐
│   Backend   │
│   FastAPI   │
└──────┬──────┘
       │
       │ 2. Проверка логина/пароля
       │ 3. Создание JWT токена
       │
       │ 4. Возврат токена
       ▼
┌─────────────┐
│   Android   │
│   App       │
└──────┬──────┘
       │
       │ 5. Сохранение токена в SharedPreferences
       │
       │ 6. GET /products (с токеном в заголовке)
       ▼
┌─────────────┐
│   Backend   │
│   FastAPI   │
└──────┬──────┘
       │
       │ 7. Проверка токена
       │ 8. Декодирование токена
       │ 9. Получение user_id
       │ 10. Получение пользователя из БД
       │
       │ 11. Возврат данных
       ▼
┌─────────────┐
│   Android   │
│   App       │
└─────────────┘
```

---

## 🛠️ Реализация в вашем проекте

### Backend (Python/FastAPI)

**Файл:** `python/main.py`

**Ключевые компоненты:**
1. **Настройки JWT (строки 27-30):**
   ```python
   SECRET_KEY = os.getenv("SECRET_KEY", "...")
   ALGORITHM = os.getenv("JWT_ALGORITHM", "HS256")
   ACCESS_TOKEN_EXPIRE_MINUTES = 30
   ```

2. **Создание токена (строки 92-100):**
   ```python
   def create_access_token(data: dict, expires_delta: Optional[timedelta] = None)
   ```

3. **Проверка токена (строки 103-111):**
   ```python
   def verify_token(token: str, credentials_exception)
   ```

4. **Получение текущего пользователя (строки 114-124):**
   ```python
   def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db))
   ```

5. **Эндпоинт логина (строки 269-298):**
   ```python
   @app.post("/login", response_model=schemas.TokenResponse)
   def come_in(user: schemas.UserLogin, db: Session = Depends(get_db))
   ```

### Android (Kotlin)

**Файлы:**
1. **`AuthManager.kt`** - Управление токенами
   - Сохранение токена
   - Получение токена
   - Проверка авторизации
   - Выход из аккаунта

2. **`RetrofitClient.kt`** - HTTP клиент
   - Interceptor для автоматического добавления токена
   - Инициализация с AuthManager

3. **`Auth.kt`** - Экран авторизации
   - Отправка запроса на логин
   - Сохранение токена после успешного входа

---

## 🧪 Пример использования

### Создание защищенного эндпоинта

**Backend:**
```python
@app.get("/user/profile")
def get_profile(current_user: User = Depends(get_current_user)):
    """
    Защищенный эндпоинт - требует валидный JWT токен
    """
    return {
        "id": current_user.id_user,
        "login": current_user.login,
        "email": current_user.email
    }
```

**Android:**
```kotlin
// Токен автоматически добавляется в запрос!
RetrofitClient.apiService.getProfile().enqueue(...)
```

---

## 🔄 Обновление токена (Refresh Token)

**Текущая реализация:** Токен истекает через 30 минут, пользователь должен логиниться снова.

**Улучшение (опционально):**
- Добавить Refresh Token (долгоживущий токен)
- Access Token (короткоживущий, 15 минут)
- При истечении Access Token использовать Refresh Token для получения нового

---

## 📝 Резюме

**JWT в вашем проекте:**

1. ✅ **Создание токена** - при логине на сервере
2. ✅ **Сохранение токена** - в SharedPreferences на Android
3. ✅ **Автоматическое добавление** - через Interceptor в Retrofit
4. ✅ **Проверка токена** - на сервере при каждом запросе
5. ✅ **Защищенные эндпоинты** - через `Depends(get_current_user)`

**Преимущества:**
- Безопасная авторизация
- Удобство для пользователя
- Масштабируемость
- Stateless архитектура

---

## 🎓 Дополнительные ресурсы

- [JWT.io](https://jwt.io) - Декодер и информация о JWT
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/) - Документация FastAPI
- [OAuth2](https://oauth.net/2/) - Стандарт авторизации

---

*Документ создан для проекта Mobilki*
*Версия: 1.0*

