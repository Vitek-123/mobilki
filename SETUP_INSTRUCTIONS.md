# Инструкции по настройке проекта

## 🔧 Настройка Backend (FastAPI)

### 1. Установка зависимостей

```bash
cd python
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `python/` на основе `python/.env.example`:

```bash
# Скопируйте пример
cp python/.env.example python/.env
```

Отредактируйте `python/.env` и укажите свои значения:

```env
# База данных
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/project_mobilki_aviasales

# JWT секретный ключ (сгенерируйте свой случайный ключ минимум 32 символа)
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# Алгоритм JWT
JWT_ALGORITHM=HS256

# Время жизни токена в минутах
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS origins (разделенные запятыми)
# Для Android эмулятора добавьте: http://10.0.2.2:8000
# Для реального устройства добавьте IP вашего компьютера: http://192.168.1.XXX:8000
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://10.0.2.2:8000
```

**Важно:** 
- Сгенерируйте свой SECRET_KEY для продакшена
- Добавьте IP адрес вашего компьютера в CORS_ORIGINS для доступа с Android устройства

### 3. Запуск сервера

```bash
cd python
python main.py
```

Сервер запустится на `http://0.0.0.0:8000`

---

## 📱 Настройка Android приложения

### 1. Настройка BASE_URL

Откройте `app/src/main/java/com/example/test/RetrofitClient.kt` и измените `BASE_URL`:

- **Для эмулятора:** `http://10.0.2.2:8000/`
- **Для реального устройства:** `http://YOUR_COMPUTER_IP:8000/` (например, `http://192.168.1.100:8000/`)

### 2. Сборка приложения

```bash
./gradlew assembleDebug
```

### 3. Установка на устройство

```bash
./gradlew installDebug
```

---

## ✅ Что было исправлено

### Backend:
- ✅ Добавлена JWT аутентификация
- ✅ Учетные данные БД вынесены в переменные окружения
- ✅ Исправлены CORS настройки (убрано `allow_origins=["*"]`)
- ✅ Эндпоинт `/login` теперь возвращает JWT токен

### Android:
- ✅ Добавлен AuthManager для управления сессией пользователя
- ✅ RetrofitClient обновлен для автоматической отправки JWT токена
- ✅ Реализована детальная страница товара (ProductDetailActivity)
- ✅ Реализована страница сравнения магазинов (ShopComparisonActivity)
- ✅ Сессия пользователя сохраняется после входа

---

## 🔐 Безопасность

### Важно для продакшена:

1. **SECRET_KEY:** Используйте случайный ключ минимум 32 символа
2. **CORS:** Укажите только разрешенные домены
3. **HTTPS:** Используйте HTTPS в продакшене
4. **База данных:** Не храните пароли в открытом виде (уже реализовано с bcrypt)

---

## 🐛 Решение проблем

### Проблема: Android не может подключиться к серверу

**Решение:**
1. Убедитесь, что сервер запущен
2. Проверьте, что IP адрес в `RetrofitClient.kt` правильный
3. Добавьте IP адрес в `CORS_ORIGINS` в `.env` файле
4. Проверьте, что устройство и компьютер в одной сети

### Проблема: Ошибка 401 Unauthorized

**Решение:**
1. Убедитесь, что токен сохраняется после входа
2. Проверьте, что `SECRET_KEY` одинаковый на сервере
3. Проверьте срок действия токена (по умолчанию 30 минут)

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверьте, что MySQL запущен
2. Убедитесь, что `DATABASE_URL` в `.env` правильный
3. Проверьте права доступа пользователя БД

---

## 📝 Дополнительные улучшения (опционально)

1. Добавить избранное
2. Добавить историю цен с графиком
3. Добавить уведомления о снижении цен
4. Добавить фильтры и сортировку
5. Добавить офлайн кэширование

