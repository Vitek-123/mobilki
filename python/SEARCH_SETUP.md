# Настройка поиска по Яндекс.Маркет

Поиск по Яндекс.Маркет настроен и готов к использованию!

## Что настроено

✅ **Selenium** - для работы с JavaScript-страницами  
✅ **Парсер** - для извлечения товаров из HTML  
✅ **Логирование** - подробные логи для диагностики  
✅ **Кэширование** - для ускорения повторных запросов  

## Как протестировать поиск

### Вариант 1: Через API (рекомендуется)

1. Запустите сервер:
   ```bash
   python main.py
   ```

2. Откройте в браузере или используйте curl:
   ```
   http://localhost:8000/products?search=смартфон&limit=10
   ```

3. Проверьте логи сервера - там будет подробная информация о поиске

### Вариант 2: Через тестовый скрипт

Запустите тестовый скрипт:

```bash
python test_search.py
```

Скрипт протестирует поиск по нескольким запросам и покажет результаты.

## Параметры поиска

Поиск использует следующие параметры:
- **Сортировка**: по цене (от дешевых к дорогим)
- **Фильтр**: только товары в наличии
- **Лимит**: до 30 товаров на запрос

## Что происходит при поиске

1. **Проверка кэша** - если запрос был недавно, возвращаются закэшированные данные
2. **Попытка через API** - если настроен OAuth токен
3. **Попытка через парсер** - использует Selenium или обычные HTTP-запросы
4. **Агрегация** - товары группируются по названию
5. **Возврат результатов** - товары возвращаются клиенту

## Логирование

В логах вы увидите:
- 🔍 Начало поиска
- 📡 Попытка через API
- 🕷️ Попытка через парсер
- ✅ Успешные результаты
- ⚠️ Предупреждения
- ❌ Ошибки

## Решение проблем

### Товары не находятся

1. **Проверьте логи** - там будет информация о том, что происходит
2. **Убедитесь, что Selenium работает**:
   ```bash
   python test_search.py
   ```
3. **Проверьте .env файл**:
   ```
   USE_SELENIUM_FOR_PARSING=true
   ```

### Медленный поиск

- Это нормально при первом запросе (Selenium запускает браузер)
- Последующие запросы будут быстрее благодаря кэшированию
- Можно отключить кэш для тестирования: `use_cache=false`

### Ошибки Selenium

- Убедитесь, что Chrome установлен
- Проверьте, что `webdriver-manager` установлен
- Смотрите логи для подробностей

## Примеры запросов

```
GET /products?search=смартфон&limit=10
GET /products?search=наушники&limit=20
GET /products?search=ноутбук&limit=15
```

## Что дальше

После настройки поиск будет работать автоматически:
- При поиске в приложении (через `/products?search=...`)
- При загрузке популярных товаров (через `/products/popular`)
- Товары будут отображаться с кнопкой "Купить в Яндекс.Маркете"

