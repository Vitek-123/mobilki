# 🔧 Исправление ошибки с путями в PowerShell

## Проблема

```
C:/Users/Vctor/OneDrive/Рабочий : Имя "C:/Users/Vctor/OneDrive/Рабочий" не распознано...
```

PowerShell не может обработать путь с пробелами и кириллицей.

## ✅ Решения

### Вариант 1: Заключить путь в кавычки

```powershell
& "C:/Users/Vctor/OneDrive/Рабочий стол/дз/3 курс/02.01 Эдгар/проект/Технологии_ПО/.venv/Scripts/python.exe" "C:\mobilki\python\main.py"
```

### Вариант 2: Использовать короткие имена (8.3 формат)

```powershell
# Найти короткое имя
cmd /c dir /x "C:\Users\Vctor\OneDrive"

# Использовать короткое имя (например, RABOCH~1)
```

### Вариант 3: Использовать переменную окружения

```powershell
$pythonPath = "C:/Users/Vctor/OneDrive/Рабочий стол/дз/3 курс/02.01 Эдгар/проект/Технологии_ПО/.venv/Scripts/python.exe"
& $pythonPath "C:\mobilki\python\main.py"
```

### Вариант 4: Использовать системный Python (рекомендуется)

Если у вас установлен Python в системе, используйте его:

```powershell
cd C:\mobilki\python
python main.py
```

### Вариант 5: Создать виртуальное окружение в проекте

**Лучшее решение!** Создайте виртуальное окружение прямо в папке проекта:

```powershell
cd C:\mobilki\python
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

## 🎯 Рекомендуемое решение

**Используйте системный Python или создайте venv в проекте:**

```powershell
# Перейти в папку проекта
cd C:\mobilki\python

# Использовать системный Python (уже установлен)
python main.py

# ИЛИ создать виртуальное окружение в проекте
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

## ⚠️ Важно

1. **Пути с пробелами** всегда нужно заключать в кавычки
2. **Кириллица в путях** может вызывать проблемы - лучше использовать латиницу
3. **Виртуальное окружение** лучше создавать в папке проекта, а не в другом месте

## 🔍 Проверка Python

Узнайте, какой Python используется:

```powershell
where.exe python
python --version
```

