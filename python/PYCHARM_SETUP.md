# 🐍 Настройка PyCharm для запуска main.py

## Проблема

Программа запускается из консоли, но не запускается из PyCharm.

## ✅ Решение

### Шаг 1: Проверьте интерпретатор Python

1. Откройте PyCharm
2. Перейдите в **File → Settings** (или `Ctrl+Alt+S`)
3. Выберите **Project: mobilki → Python Interpreter**
4. Убедитесь, что выбран правильный интерпретатор:
   - **Системный Python**: `C:\Python312\python.exe`
   - **ИЛИ виртуальное окружение проекта**: `C:\mobilki\python\venv\Scripts\python.exe`

### Шаг 2: Создайте виртуальное окружение в проекте (рекомендуется)

```powershell
cd C:\mobilki\python
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Затем в PyCharm:
1. **File → Settings → Project: mobilki → Python Interpreter**
2. Нажмите на шестеренку ⚙️ → **Add...**
3. Выберите **Existing environment**
4. Укажите путь: `C:\mobilki\python\venv\Scripts\python.exe`
5. Нажмите **OK**

### Шаг 3: Настройте конфигурацию запуска

1. Откройте `main.py`
2. Нажмите на стрелку рядом с кнопкой запуска (▶️) → **Edit Configurations...**
3. Или: **Run → Edit Configurations...**

**Настройки конфигурации:**

- **Name**: `main`
- **Script path**: `C:\mobilki\python\main.py`
- **Python interpreter**: Выберите правильный интерпретатор (см. Шаг 1)
- **Working directory**: `C:\mobilki\python`
- **Environment variables**: (если нужны)
  ```
  DATABASE_URL=mysql+pymysql://user:password@localhost/dbname
  REDIS_URL=redis://localhost:6379
  WB_REQUEST_INTERVAL=2.0
  ```

### Шаг 4: Альтернатива - запуск через uvicorn

Если используете FastAPI, можно настроить запуск через uvicorn:

1. **Run → Edit Configurations...**
2. Нажмите **+** → **Python**
3. Настройки:
   - **Name**: `uvicorn`
   - **Script path**: `C:\mobilki\python\venv\Scripts\uvicorn.exe` (или системный)
   - **Parameters**: `main:app --reload --host 0.0.0.0 --port 8000`
   - **Working directory**: `C:\mobilki\python`

### Шаг 5: Проверьте переменные окружения

Если в коде используются переменные окружения (`.env` файл):

1. Убедитесь, что файл `.env` находится в `C:\mobilki\python\`
2. В конфигурации запуска добавьте:
   - **Environment variables**: `PYTHONPATH=C:\mobilki\python`

## 🔍 Диагностика проблем

### Проблема 1: "ModuleNotFoundError"

**Решение:**
- Проверьте, что выбран правильный интерпретатор
- Убедитесь, что все пакеты установлены: `pip install -r requirements.txt`

### Проблема 2: "No module named 'pywin32_bootstrap'"

**Решение:**
- Это предупреждение можно игнорировать
- Или переустановите pywin32: `pip install --force-reinstall pywin32`

### Проблема 3: "Cannot connect to MySQL"

**Решение:**
- Это нормально, если MySQL не запущен
- Приложение будет работать, но функции с БД будут недоступны

### Проблема 4: Путь с кириллицей в интерпретаторе

**Решение:**
- НЕ используйте виртуальное окружение из другого проекта
- Создайте новое в `C:\mobilki\python\venv`

## 📝 Быстрая проверка

1. В PyCharm откройте терминал: **View → Tool Windows → Terminal**
2. Выполните:
   ```powershell
   python --version
   python -c "import sys; print(sys.executable)"
   ```
3. Убедитесь, что путь совпадает с интерпретатором в настройках

## 🎯 Рекомендуемая конфигурация

```
Интерпретатор: C:\mobilki\python\venv\Scripts\python.exe
Working directory: C:\mobilki\python
Script path: C:\mobilki\python\main.py
```

## ⚠️ Важно

- **Всегда используйте виртуальное окружение проекта**, а не из другого проекта
- **Working directory** должен быть `C:\mobilki\python`
- **Проверьте**, что все зависимости установлены в выбранном интерпретаторе


