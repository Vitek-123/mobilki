# ⚡ Быстрое исправление запуска в PyCharm

## 🔴 Проблема
Программа запускается из консоли, но не запускается из PyCharm.

## ✅ Решение за 3 шага

### Шаг 1: Проверьте интерпретатор

1. **File → Settings** (`Ctrl+Alt+S`)
2. **Project: mobilki → Python Interpreter**
3. Убедитесь, что выбран: **`C:\Python312\python.exe`** (системный Python)

**Если там указан путь с кириллицей** (например, `C:/Users/Vctor/OneDrive/Рабочий стол/...`):
- Нажмите на шестеренку ⚙️ → **Add...**
- Выберите **System Interpreter**
- Укажите: `C:\Python312\python.exe`
- Нажмите **OK**

### Шаг 2: Настройте конфигурацию запуска

1. Откройте `main.py`
2. Нажмите на стрелку рядом с ▶️ → **Edit Configurations...**
3. Или: **Run → Edit Configurations...**

**Проверьте:**
- **Script path**: `C:\mobilki\python\main.py`
- **Python interpreter**: `C:\Python312\python.exe` (или ваш системный Python)
- **Working directory**: `C:\mobilki\python` ⚠️ **ВАЖНО!**

### Шаг 3: Проверьте терминал PyCharm

1. Откройте терминал в PyCharm: **View → Tool Windows → Terminal**
2. Выполните:
   ```powershell
   cd C:\mobilki\python
   python --version
   python -c "import sys; print(sys.executable)"
   ```
3. Убедитесь, что путь совпадает с интерпретатором в настройках

## 🎯 Рекомендуемая конфигурация

```
Name: main
Script path: C:\mobilki\python\main.py
Python interpreter: C:\Python312\python.exe
Working directory: C:\mobilki\python
```

## ⚠️ Частые ошибки

### Ошибка 1: "ModuleNotFoundError"
**Причина:** PyCharm использует другой интерпретатор  
**Решение:** См. Шаг 1

### Ошибка 2: "No module named 'pywin32_bootstrap'"
**Причина:** Предупреждение, можно игнорировать  
**Решение:** Это не критично, приложение должно работать

### Ошибка 3: Путь с кириллицей
**Причина:** PyCharm пытается использовать виртуальное окружение из другого проекта  
**Решение:** Используйте системный Python или создайте venv в проекте

## 🔍 Проверка

После настройки запустите `main.py` из PyCharm. Должно появиться:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Если видите это - всё работает! ✅


