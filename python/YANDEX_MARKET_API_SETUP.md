# 🔧 Настройка доступа к Яндекс.Маркет API

## 📝 Шаг 1: Создание приложения

### Поле "Название доступа":
**Что писать:** Любое понятное название, например:
- `Mobilki App`
- `Market Data Access`
- `Mobilki Market Parser`
- `Техника API`

Это название только для вашего удобства, оно не влияет на работу API.

---

## 🔑 Шаг 2: Выбор прав доступа

### Для Яндекс.Маркет нужны следующие права:

**Основные права:**
- `market:read` - Чтение данных Яндекс.Маркет
- `market:content` - Доступ к контенту Яндекс.Маркет

**Если этих прав нет в списке:**

1. **Проверьте тип приложения:**
   - Должно быть выбрано: **"Веб-сервисы"** или **"Серверное приложение"**
   - Не выбирайте "Мобильное приложение" или "Десктопное приложение"

2. **Права могут называться по-другому:**
   - Ищите что-то вроде "Доступ к Яндекс.Маркет"
   - Или "Чтение данных Маркета"
   - Или просто "Маркет"

3. **Если прав нет в списке:**
   - Возможно, нужно сначала зарегистрироваться в партнерской программе
   - Или использовать другой способ доступа

---

## 🔄 Альтернативный способ: Content API

Если через OAuth не получается, можно использовать **Content API** без OAuth:

### Вариант 1: Публичный Content API
```
https://api.content.market.yandex.ru/v1/search.json?text=смартфон&geo_id=213
```

**Не требует:**
- OAuth токена
- Регистрации приложения
- Прав доступа

**Ограничения:**
- Может быть ограниченное количество запросов
- Меньше данных

### Вариант 2: Partner API (требует регистрации)
```
https://api.partner.market.yandex.ru/
```

**Требует:**
- Регистрацию в партнерской программе
- API ключ

---

## 📋 Пошаговая инструкция

### Если используете OAuth:

1. **Создайте приложение:**
   - Название: `Mobilki App` (или любое другое)
   - Тип: **Веб-сервисы**
   - Callback URI: `http://localhost:8000/oauth/callback`

2. **Выберите права:**
   - Ищите права, связанные с "Маркет" или "Market"
   - Если нет - попробуйте без прав (для публичного API)

3. **Сохраните:**
   - Client ID
   - Client Secret

4. **Получите токен:**
   - Используйте OAuth 2.0 flow
   - Или используйте публичный API без токена

---

## 🚀 Быстрый старт: Публичный API (без регистрации)

Если регистрация через OAuth сложная, можно начать с публичного API:

```python
import requests

# Публичный API - не требует токена
url = "https://api.content.market.yandex.ru/v1/search.json"
params = {
    "text": "смартфон",
    "geo_id": 213,  # Москва
    "page": 1
}

response = requests.get(url, params=params)
data = response.json()
```

**Попробуйте сначала этот способ!**

---

## ❓ Если ничего не работает

1. **Используйте публичный API** (см. выше)
2. **Или парсинг HTML** страниц Яндекс.Маркет
3. **Или YML фиды** от магазинов

---

## 💡 Рекомендация

**Начните с публичного API:**
- Не требует регистрации
- Работает сразу
- Можно протестировать

**Если нужны расширенные возможности:**
- Тогда регистрируйтесь в партнерской программе
- Или используйте OAuth с правами

