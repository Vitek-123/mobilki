# ✅ Решение проблемы invalid_scope

## 🔍 Проблема

Ошибка `invalid_scope` возникает потому, что:
1. При создании приложения вы выбрали права "Яндекс.Маркет / Поиск по товарам"
2. Но при запросе токена мы указываем scope `market:read market:content`, который не соответствует

## ✅ Решение

### Вариант 1: Получить токен БЕЗ scope (Рекомендуется)

Права уже указаны при создании приложения, поэтому **не нужно указывать scope** в запросе.

**Обновленный скрипт:**
```bash
python get_yandex_market_token.py
```

Теперь он запрашивает токен **без scope** - использует права, указанные при создании приложения.

---

### Вариант 2: Использовать парсинг HTML ⭐⭐⭐⭐⭐

**Это самый простой и надежный способ!**

OAuth для Яндекс.Маркет может быть сложным. Вместо этого используйте парсинг HTML страниц:

```bash
python yandex_market_parser.py
```

**Преимущества:**
- ✅ Работает сразу
- ✅ Не требует OAuth
- ✅ Не требует токенов
- ✅ Доступны все данные

---

### Вариант 3: Использовать response_type=token

Попробуйте получить токен напрямую через `response_type=token`:

1. Откройте в браузере:
```
https://oauth.yandex.ru/authorize?response_type=token&client_id=5c11d50c52304b16b6c07fbb6b5382d8
```

2. Авторизуйтесь
3. Разрешите доступ
4. Токен будет в URL после `access_token=`

---

## 🎯 Рекомендация

**Используйте парсинг HTML** - это самый простой способ:

1. Запустите: `python yandex_market_parser.py`
2. Протестируйте получение данных
3. Интегрируйте в проект

**OAuth можно настроить позже, если понадобится.**

---

## 📝 Что делать сейчас

1. **Попробуйте обновленный скрипт** (без scope):
   ```bash
   python get_yandex_market_token.py
   ```

2. **Или используйте парсинг** (проще и надежнее):
   ```bash
   python yandex_market_parser.py
   ```

3. **Или получите токен вручную** через браузер (см. Вариант 3 выше)

