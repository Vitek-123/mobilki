# 🔧 Исправление ошибки invalid_scope

## ❌ Проблема

Ошибка `invalid_scope` означает, что указанные права доступа (`market:read market:content`) неверны или не поддерживаются через OAuth для вашего приложения.

## 🔍 Причина

Яндекс.Маркет API может требовать:
1. **Другой формат scope** - не `market:read`, а что-то другое
2. **Регистрацию в партнерской программе** - OAuth может не работать без этого
3. **Использование API-Key вместо OAuth** - OAuth токен устарел для Маркета

## ✅ Решения

### Решение 1: Попробовать без scope

Обновлен скрипт `get_yandex_market_token.py` - теперь он пытается получить токен без указания scope.

**Попробуйте снова:**
```bash
python get_yandex_market_token.py
```

### Решение 2: Использовать парсинг HTML (Рекомендуется) ⭐⭐⭐⭐⭐

OAuth для Яндекс.Маркет может не работать без регистрации в партнерской программе. 

**Лучше использовать парсинг HTML страниц:**

```bash
python yandex_market_parser.py
```

**Преимущества:**
- ✅ Работает сразу
- ✅ Не требует OAuth
- ✅ Не требует регистрации
- ✅ Доступны все данные

### Решение 3: Регистрация в партнерской программе

Для полноценного API доступа нужно:
1. Зарегистрироваться на https://partner.market.yandex.ru/
2. Получить API-Key токен (не OAuth)
3. Использовать API-Key в заголовках запросов

**См. инструкцию:** `YANDEX_MARKET_PARTNER_REGISTRATION.md`

---

## 🎯 Рекомендация

**Используйте парсинг HTML** - это самый простой и надежный способ:

1. Запустите: `python yandex_market_parser.py`
2. Протестируйте получение данных
3. Интегрируйте в ваш проект

**OAuth для Яндекс.Маркет сложен и может не работать без партнерской программы.**

---

## 📝 Что делать сейчас

1. **Попробуйте обновленный скрипт** (без scope):
   ```bash
   python get_yandex_market_token.py
   ```

2. **Если не работает - используйте парсинг:**
   ```bash
   python yandex_market_parser.py
   ```

3. **Или зарегистрируйтесь в партнерской программе** для API-Key

