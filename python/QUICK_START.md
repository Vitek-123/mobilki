# 🚀 Быстрый старт

## ✅ Зависимости установлены!

Все основные зависимости проверены и работают:
- ✅ FastAPI
- ✅ Pydantic
- ✅ Pydantic Core
- ✅ SQLAlchemy
- ✅ Redis (опционально)
- ✅ Selenium (опционально)
- ✅ pywin32 (опционально, только для Windows)

## 🏃 Запуск сервера

### Вариант 1: Через Python
```bash
cd C:\mobilki\python
python main.py
```

### Вариант 2: Через uvicorn (рекомендуется)
```bash
cd C:\mobilki\python
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Вариант 3: Через uvicorn с автоматическим перезапуском
```bash
cd C:\mobilki\python
uvicorn main:app --reload
```

## 📝 Проверка работы

После запуска сервера откройте в браузере:
- **API документация**: http://localhost:8000/docs
- **Альтернативная документация**: http://localhost:8000/redoc
- **Главная страница**: http://localhost:8000

## ⚙️ Настройка

Создайте файл `.env` в папке `python/` (если еще не создан):

```env
# База данных
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/project_mobilki_aviasales

# Redis (опционально)
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
CACHE_TTL=10800

# Wildberries API
WB_REQUEST_INTERVAL=2.0

# Selenium (опционально)
USE_SELENIUM=auto
CHROME_BIN=C:\Program Files\Google\Chrome\Application\chrome.exe
```

## 🔧 Устранение проблем

### Если сервер не запускается:

1. **Проверьте зависимости:**
   ```bash
   python test_imports.py
   ```

2. **Проверьте подключение к БД:**
   - Убедитесь, что MySQL запущен
   - Проверьте настройки в `.env`

3. **Проверьте порт:**
   - Убедитесь, что порт 8000 свободен
   - Или измените порт в `main.py` (строка 1209)

### Если есть ошибки с pywin32:

```bash
python -m pywin32_postinstall -install
```

## 📚 Дополнительная информация

- **Документация по Selenium**: `SELENIUM_FULL_SETUP.md`
- **Настройки частоты запросов**: `WILDBERRIES_RATE_LIMIT.md`
- **Исправление зависимостей**: `FIX_DEPENDENCIES.md`

