# 🐍 Настройка виртуального окружения

## Проблема

Используется виртуальное окружение из другого проекта с несовместимыми версиями пакетов.

## ✅ Решение: Создать новое виртуальное окружение

### Шаг 1: Создать виртуальное окружение

```bash
cd C:\mobilki\python
python -m venv venv
```

### Шаг 2: Активировать виртуальное окружение

**Windows (PowerShell):**
```powershell
venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
venv\Scripts\activate.bat
```

### Шаг 3: Установить зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Исправить pywin32 (опционально, только для Windows)

```bash
python -m pywin32_postinstall -install
```

### Шаг 5: Проверить установку

```bash
python test_imports.py
```

### Шаг 6: Запустить сервер

```bash
python main.py
```

Или через uvicorn:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 📝 Быстрая команда (все сразу)

```powershell
cd C:\mobilki\python
python -m venv venv
venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
python -m pywin32_postinstall -install
python test_imports.py
python main.py
```

## ⚠️ Важно

1. **Активируйте виртуальное окружение** перед запуском `main.py`
2. **pywin32 не обязателен** - предупреждение можно игнорировать
3. **MySQL не обязателен** для тестирования API (только для функций с БД)

## 🔍 Проверка

После активации виртуального окружения в начале строки должно быть `(venv)`:
```
(venv) PS C:\mobilki\python>
```

