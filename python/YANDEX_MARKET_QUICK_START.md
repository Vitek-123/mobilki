# 🚀 Быстрый старт: Яндекс.Маркет API

## ✅ У вас уже есть:

- **Client ID:** `5c11d50c52304b16b6c07fbb6b5382d8`
- **Client Secret:** `63f8b8c7e1d74543affdab9cde21c14b`
- **Redirect URI:** `https://oauth.yandex.ru/verification_code`
- **Права:** Яндекс.Маркет / Поиск по товарам ✅

---

## 📝 Шаг 1: Сохраните данные в .env

Создайте файл `python/.env` и добавьте:

```env
# Яндекс.Маркет OAuth данные
YANDEX_MARKET_CLIENT_ID=5c11d50c52304b16b6c07fbb6b5382d8
YANDEX_MARKET_CLIENT_SECRET=63f8b8c7e1d74543affdab9cde21c14b
YANDEX_MARKET_REDIRECT_URI=https://oauth.yandex.ru/verification_code

# OAuth токен (получите на следующем шаге)
YANDEX_MARKET_OAUTH_TOKEN=
```

---

## 🔑 Шаг 2: Получите OAuth токен

Запустите скрипт:

```bash
python get_yandex_market_token.py
```

**Что произойдет:**
1. Откроется браузер с формой авторизации Яндекс
2. Войдите в свой Яндекс аккаунт
3. Разрешите доступ приложению
4. Скопируйте код из адресной строки (параметр `code`)
5. Вставьте код в консоль
6. Скрипт получит токен и сохранит его в `.env`

---

## 🧪 Шаг 3: Протестируйте API

После получения токена запустите:

```bash
python test_yandex_market_api.py
```

Это проверит, работает ли API с вашим токеном.

---

## 💻 Использование в коде

```python
import requests
import os
from dotenv import load_dotenv

load_dotenv()

OAUTH_TOKEN = os.getenv("YANDEX_MARKET_OAUTH_TOKEN")

headers = {
    "Authorization": f"OAuth {OAUTH_TOKEN}",
    "Content-Type": "application/json"
}

# Поиск товаров
response = requests.get(
    "https://api.content.market.yandex.ru/v1/search.json",
    headers=headers,
    params={
        "text": "смартфон",
        "geo_id": 213,  # Москва
        "page": 1
    }
)

data = response.json()
print(data)
```

---

## ⚠️ Важно

1. **Токен действителен ограниченное время** - обычно несколько часов/дней
2. **При истечении токена** - запустите `get_yandex_market_token.py` снова
3. **Не публикуйте токен** - храните его только в `.env` файле
4. **`.env` в `.gitignore`** - не коммитьте секретные данные

---

## 🔄 Если токен не работает

1. Проверьте, что токен не истек
2. Получите новый токен через `get_yandex_market_token.py`
3. Или используйте парсинг HTML (см. `yandex_market_parser.py`)

---

## 📚 Документация

- **Официальная документация:** https://yandex.ru/dev/market/
- **OAuth документация:** https://yandex.ru/dev/id/doc/ru/

