# 🔧 Решение проблемы timeout подключения

## Проблема
Android приложение не может подключиться к серверу - `SocketTimeoutException: timeout`

## Возможные причины

### 1. Неправильный IP адрес
IP адрес `172.20.10.2` может быть неверным или изменился.

**Решение:**
1. Узнайте текущий IP вашего компьютера:
   ```bash
   # Windows
   ipconfig
   # Найдите IPv4 адрес (обычно 192.168.x.x или 172.x.x.x)
   ```

2. Обновите IP в `RetrofitClient.kt`:
   ```kotlin
   private const val BASE_URL = "http://ВАШ_IP:8000/"
   ```

### 2. Сервер не запущен
Сервер может быть не запущен или остановился.

**Решение:**
```bash
cd c:\mobilki\python
uvicorn main:app --reload --host 0.0.0.0
```

**Важно:** Используйте `--host 0.0.0.0` чтобы сервер был доступен из сети!

### 3. Брандмауэр блокирует подключение
Windows Firewall может блокировать порт 8000.

**Решение:**
1. Откройте "Брандмауэр Защитника Windows"
2. "Дополнительные параметры" → "Правила для входящих подключений"
3. "Создать правило" → "Порт" → TCP → 8000 → "Разрешить подключение"

Или через PowerShell (от администратора):
```powershell
New-NetFirewallRule -DisplayName "Python FastAPI" -Direction Inbound -LocalPort 8000 -Protocol TCP -Action Allow
```

### 4. Эмулятор Android
Если используете эмулятор, нужен специальный IP.

**Решение:**
- Для эмулятора Android используйте: `http://10.0.2.2:8000/`
- Для реального устройства: используйте IP вашего компьютера в локальной сети

### 5. Разные сети
Android устройство и компьютер должны быть в одной сети Wi-Fi.

**Решение:**
- Убедитесь, что оба устройства подключены к одной Wi-Fi сети
- Не используйте мобильный интернет на Android устройстве

## Быстрая проверка

### 1. Проверьте, что сервер запущен:
```bash
curl http://localhost:8000/products/popular-phones?limit=3
```

### 2. Проверьте доступность с Android устройства:
- Откройте браузер на Android устройстве/эмуляторе
- Перейдите: `http://ВАШ_IP:8000/docs`
- Должна открыться документация FastAPI

### 3. Проверьте IP адрес:
```bash
# Windows
ipconfig | findstr IPv4
```

## Исправления в коде

✅ Увеличены таймауты до 60 секунд в `RetrofitClient.kt`

## Рекомендации

1. **Для разработки:** Используйте эмулятор с `10.0.2.2`
2. **Для тестирования на реальном устройстве:** Используйте IP компьютера в локальной сети
3. **Всегда проверяйте:** Что сервер запущен с `--host 0.0.0.0`
