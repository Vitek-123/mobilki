# 🔍 Диагностика проблемы "Товары не найдены"

## Шаг 1: Проверка работы сервера

### 1.1 Убедитесь, что сервер запущен:
```bash
cd python
uvicorn main:app --reload
```

### 1.2 Проверьте эндпоинт в браузере или через curl:
```bash
# Откройте в браузере:
http://localhost:8000/products/popular-phones?limit=3&use_cache=false

# Или через curl:
curl "http://localhost:8000/products/popular-phones?limit=3&use_cache=false"
```

**Ожидаемый результат:** JSON с массивом товаров, например:
```json
{
  "products": [
    {
      "product": {
        "id_product": 123456,
        "title": "Смартфон Apple iPhone 15",
        "brand": "Apple",
        "model": "iPhone 15",
        "image": "https://...",
        "description": "..."
      },
      "prices": [
        {
          "price": 79990.0,
          "shop_name": "Wildberries",
          ...
        }
      ],
      "min_price": 79990.0,
      "max_price": 79990.0
    }
  ],
  "total": 3
}
```

**Если видите пустой массив `"products": []`** - проблема на стороне сервера (Wildberries API не возвращает данные).

---

## Шаг 2: Проверка подключения Android приложения к серверу

### 2.1 Проверьте IP адрес сервера в `RetrofitClient.kt`:
```kotlin
private const val BASE_URL = "http://172.20.10.2:8000/"
```

**Важно:** 
- IP адрес `172.20.10.2` должен быть IP вашего компьютера в локальной сети
- Убедитесь, что Android устройство/эмулятор находится в той же сети
- Проверьте, что сервер доступен по этому IP (не только localhost)

### 2.2 Как узнать правильный IP:
**Windows:**
```bash
ipconfig
# Найдите IPv4 адрес (обычно 192.168.x.x или 172.x.x.x)
```

**Linux/Mac:**
```bash
ifconfig
# или
ip addr
```

### 2.3 Проверьте доступность сервера с Android устройства:
- Откройте браузер на Android устройстве/эмуляторе
- Перейдите по адресу: `http://ВАШ_IP:8000/docs` (должна открыться документация FastAPI)

---

## Шаг 3: Проверка логов Android приложения

### 3.1 Включите логирование в Android Studio:
1. Откройте **Logcat** в Android Studio
2. Фильтр: `ProductViewModel` или `RetrofitClient`
3. Запустите приложение

### 3.2 Что искать в логах:

**Успешный запрос:**
```
ProductViewModel: Загрузка товаров. Поисковый запрос: ''
ProductViewModel: Запрос популярных телефонов...
ProductViewModel: Ответ получен. Статус: 200, Успешно: true
ProductViewModel: Количество товаров в ответе: 3
ProductViewModel: Конвертировано товаров: 3
```

**Ошибка подключения:**
```
ProductViewModel: Исключение при загрузке товаров
java.net.ConnectException: Failed to connect to 172.20.10.2:8000
```

**Пустой ответ:**
```
ProductViewModel: Количество товаров в ответе: 0
ProductViewModel: Конвертировано товаров: 0
```

**Ошибка сервера:**
```
ProductViewModel: Ошибка загрузки: 500
```

---

## Шаг 4: Проверка логов сервера

### 4.1 Смотрите логи сервера при запросе от Android:

**Успешный запрос:**
```
INFO - Запрос популярных телефонов: limit=3, use_cache=False
INFO - Начинаем поиск популярных телефонов (limit=3)
INFO - Поиск по запросу: 'смартфон'
INFO - Найдено 20 товаров по запросу 'смартфон'
INFO - Добавлен телефон: Apple iPhone 15 - 79990.0 руб.
INFO - Итого найдено 3 популярных телефонов
INFO - Возвращаем 3 товаров клиенту
```

**Проблема с Wildberries API:**
```
ERROR - Ошибка запроса к Wildberries API: ...
ERROR - Ошибка при получении популярных телефонов: ...
```

---

## Шаг 5: Быстрая проверка через тестовый скрипт

Запустите тестовый скрипт:
```bash
cd python
python test_popular_phones.py
```

Это покажет, работает ли эндпоинт локально.

---

## Возможные проблемы и решения

### Проблема 1: "Failed to connect to 172.20.10.2:8000"
**Решение:**
- Проверьте, что сервер запущен
- Проверьте правильность IP адреса
- Убедитесь, что Android устройство в той же сети
- Для эмулятора Android используйте `10.0.2.2` вместо локального IP

### Проблема 2: "Ошибка загрузки: 404"
**Решение:**
- Проверьте, что BASE_URL заканчивается на `/`
- Убедитесь, что эндпоинт `/products/popular-phones` существует

### Проблема 3: "Количество товаров в ответе: 0"
**Решение:**
- Wildberries API может быть недоступен или заблокирован
- Проверьте логи сервера для деталей
- Попробуйте увеличить timeout в RetrofitClient

### Проблема 4: "Конвертировано товаров: 0" (но в ответе есть товары)
**Решение:**
- Проблема в преобразовании данных
- Проверьте структуру ответа API
- Смотрите логи с ошибками конвертации

---

## Для эмулятора Android

Если используете эмулятор Android, замените IP на специальный адрес:

```kotlin
// Для эмулятора Android
private const val BASE_URL = "http://10.0.2.2:8000/"

// Для реального устройства в той же сети
private const val BASE_URL = "http://192.168.1.XXX:8000/" // Ваш IP
```

---

## Временное решение для тестирования

Если нужно быстро протестировать, можно временно вернуть старый эндпоинт:

В `ProductViewModel.kt` замените:
```kotlin
val response = if (searchQuery.isEmpty()) {
    // Временно используем поиск вместо популярных телефонов
    RetrofitClient.apiService.getProductsSuspend(
        skip = 0,
        limit = 3,
        search = "смартфон"
    )
} else {
    ...
}
```

Это покажет, работает ли вообще подключение к серверу.
