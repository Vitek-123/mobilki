# 🔧 Установка Redis на Windows

## ⚠️ Важно: Redis опционален!

**Приложение будет работать БЕЗ Redis**, просто без кэширования. Кэширование ускоряет повторные запросы, но не является обязательным.

---

## 🎯 Вариант 1: Без Redis (самый простой)

**Просто запустите приложение без Redis!**

Приложение автоматически определит, что Redis недоступен, и будет работать без кэширования. В логах будет предупреждение, но это нормально.

```bash
cd python
uvicorn main:app --reload
```

**Преимущества:**
- ✅ Не требует установки
- ✅ Работает сразу
- ✅ Все функции доступны

**Недостатки:**
- ❌ Нет кэширования (запросы будут медленнее при повторных обращениях)

---

## 🐳 Вариант 2: Docker Desktop (рекомендуется)

### Шаг 1: Установите Docker Desktop

1. Скачайте Docker Desktop для Windows:
   - https://www.docker.com/products/docker-desktop/

2. Установите и запустите Docker Desktop

3. Дождитесь, пока Docker запустится (иконка в трее)

### Шаг 2: Запустите Redis

```bash
docker run -d -p 6379:6379 --name redis redis:latest
```

### Шаг 3: Проверьте, что Redis работает

```bash
docker ps
```

Должна быть строка с контейнером `redis`.

---

## 🪟 Вариант 3: WSL (Windows Subsystem for Linux)

Если у вас установлен WSL:

### Шаг 1: Откройте WSL

```bash
wsl
```

### Шаг 2: Установите Redis в WSL

```bash
sudo apt-get update
sudo apt-get install redis-server
```

### Шаг 3: Запустите Redis

```bash
redis-server
```

Оставьте это окно открытым.

---

## 📦 Вариант 4: Memurai (нативный Windows Redis)

Memurai - это порт Redis для Windows.

### Шаг 1: Скачайте Memurai

- https://www.memurai.com/get-memurai

### Шаг 2: Установите Memurai

Следуйте инструкциям установщика.

### Шаг 3: Запустите Memurai

Memurai запустится как служба Windows автоматически.

---

## 🔍 Проверка работы Redis

После установки Redis, проверьте подключение:

### Способ 1: Через Python

```python
import redis
r = redis.Redis(host='localhost', port=6379, db=0)
r.ping()  # Должно вернуть True
print("Redis работает!")
```

### Способ 2: Через командную строку (если установлен redis-cli)

```bash
redis-cli ping
# Должно вернуть: PONG
```

### Способ 3: Через API

После запуска FastAPI:

```bash
curl http://localhost:8000/external/cache/stats
```

Если Redis работает, вы увидите:
```json
{
  "status": "enabled",
  "connected_clients": 1,
  ...
}
```

Если Redis не работает:
```json
{
  "status": "disabled"
}
```

---

## ⚙️ Настройка приложения

### Если Redis установлен:

В `.env` файле (или оставьте значения по умолчанию):
```env
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
CACHE_TTL=3600
```

### Если Redis НЕ установлен:

Просто не указывайте эти переменные или оставьте их пустыми. Приложение автоматически отключит кэширование.

---

## 🐛 Решение проблем

### Проблема: "docker не является внутренней или внешней командой"

**Решение:** Установите Docker Desktop (см. Вариант 2) или используйте Вариант 1 (без Redis).

### Проблема: Redis не подключается

**Проверьте:**
1. Redis запущен? (`docker ps` или проверьте службы Windows)
2. Порт 6379 свободен? (может быть занят другим приложением)
3. Правильные настройки в `.env`?

### Проблема: Ошибка при подключении к Redis

**Решение:** Приложение продолжит работать без кэширования. Это нормально.

---

## 📊 Сравнение вариантов

| Вариант | Сложность | Кэширование | Рекомендация |
|---------|-----------|-------------|--------------|
| Без Redis | ⭐ Легко | ❌ Нет | ✅ Для начала |
| Docker Desktop | ⭐⭐ Средне | ✅ Да | ✅ Рекомендуется |
| WSL | ⭐⭐ Средне | ✅ Да | Если уже есть WSL |
| Memurai | ⭐⭐ Средне | ✅ Да | Для продакшена |

---

## 🚀 Рекомендация

**Для начала:** Используйте Вариант 1 (без Redis). Приложение будет работать, просто без кэширования.

**Для продакшена:** Установите Docker Desktop и используйте Redis для лучшей производительности.

---

*Документ создан: 2024*
