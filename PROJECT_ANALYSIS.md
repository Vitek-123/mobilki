# Анализ проекта Mobilki

## 📋 Обзор проекта

Проект представляет собой мобильное приложение для сравнения цен на товары с FastAPI бэкендом и Android клиентом.

**Технологии:**
- Backend: FastAPI, SQLAlchemy, MySQL, bcrypt
- Frontend: Android (Kotlin), Retrofit, Glide, Material Design
- База данных: MySQL

---

## ✅ Что уже реализовано

### Backend (FastAPI)
- ✅ Регистрация пользователей с хешированием паролей
- ✅ Авторизация пользователей
- ✅ Получение списка продуктов с пагинацией
- ✅ Поиск продуктов по названию, бренду, модели
- ✅ Получение цен из разных магазинов
- ✅ CORS middleware для мобильного приложения

### Android App
- ✅ Экран авторизации
- ✅ Экран регистрации
- ✅ Главный экран со списком продуктов
- ✅ Поиск товаров
- ✅ Пагинация при прокрутке
- ✅ Загрузка изображений через Glide
- ✅ Bottom Navigation
- ✅ Обработка состояний загрузки/ошибок

---

## 🔴 Критические проблемы

### 1. Безопасность

#### Проблема: Отсутствие JWT аутентификации
**Текущее состояние:** После логина пользователь просто получает данные, но нет токена для последующих запросов.

**Решение:**
- Добавить JWT токены при логине
- Сохранять токен в SharedPreferences
- Добавить interceptor в Retrofit для автоматической отправки токена
- Создать защищенные эндпоинты с проверкой токена

#### Проблема: CORS разрешает все домены
**Файл:** `python/main.py:35`
```python
allow_origins=["*"]  # ОПАСНО!
```

**Решение:**
```python
allow_origins=[
    "http://localhost:8000",
    "http://127.0.0.1:8000",
    # Добавить конкретные домены для продакшена
]
```

#### Проблема: Учетные данные БД в коде
**Файл:** `python/database.py:5`
```python
sql_url_db = "mysql+pymysql://root:root@localhost:3306/project_mobilki_aviasales"
```

**Решение:**
- Использовать переменные окружения
- Создать `.env` файл (не коммитить в git!)
- Использовать библиотеку `python-dotenv`

#### Проблема: Нет сохранения сессии пользователя
**Текущее состояние:** После перезапуска приложения пользователь снова должен логиниться.

**Решение:**
- Сохранять токен в SharedPreferences
- Проверять токен при запуске приложения
- Автоматически логинить пользователя, если токен валиден

---

### 2. Недостающие функции

#### TODO в коде:
1. **Детальная страница товара** (`Main.kt:81`)
   - Показать полную информацию о товаре
   - Показать все цены из разных магазинов
   - История изменения цен (график)
   - Кнопка "Добавить в избранное"

2. **Страница сравнения магазинов** (`Main.kt:86`)
   - Список всех магазинов с ценами
   - Сортировка по цене
   - Переход на сайт магазина

3. **Избранное** (`MenuActivity.kt:38`)
   - Добавление товаров в избранное
   - Список избранных товаров
   - Уведомления об изменении цен

4. **Настройки** (`Settings.kt`)
   - Профиль пользователя
   - Выход из аккаунта
   - Настройки уведомлений
   - О приложении

---

## ⚠️ Важные улучшения

### Backend

1. **Валидация данных**
   - Валидация email при регистрации
   - Минимальная длина пароля
   - Валидация формата данных

2. **Обработка ошибок**
   - Единый формат ошибок
   - Логирование ошибок
   - Обработка исключений БД

3. **Дополнительные эндпоинты**
   - `GET /products/{id}/prices/history` - история цен
   - `POST /favorites` - добавить в избранное
   - `GET /favorites` - получить избранное
   - `DELETE /favorites/{id}` - удалить из избранного
   - `GET /user/profile` - профиль пользователя
   - `PUT /user/profile` - обновить профиль

4. **Фильтры и сортировка**
   - Фильтр по бренду
   - Фильтр по цене (мин/макс)
   - Сортировка по цене, названию, дате

### Android App

1. **Улучшение UX**
   - Pull-to-refresh на главном экране
   - Debounce для поиска (задержка 500мс)
   - Улучшенная обработка пустых состояний
   - Анимации переходов

2. **Кэширование**
   - Кэширование продуктов в Room Database
   - Офлайн режим (показывать кэшированные данные)
   - Обновление при появлении интернета

3. **Конфигурация**
   - Вынести BASE_URL в `buildConfigField` или `strings.xml`
   - Разные URL для debug/release

4. **Валидация на клиенте**
   - Валидация email при регистрации
   - Проверка силы пароля
   - Визуальная обратная связь

---

## 📝 Рекомендации по приоритетам

### Высокий приоритет (критично)
1. ✅ Добавить JWT аутентификацию
2. ✅ Сохранять сессию пользователя
3. ✅ Исправить CORS настройки
4. ✅ Вынести учетные данные БД в переменные окружения
5. ✅ Реализовать детальную страницу товара
6. ✅ Реализовать страницу сравнения магазинов

### Средний приоритет (важно)
1. ✅ Добавить избранное
2. ✅ Реализовать настройки/профиль
3. ✅ Добавить фильтры и сортировку
4. ✅ Улучшить обработку ошибок
5. ✅ Добавить валидацию данных

### Низкий приоритет (желательно)
1. ✅ История цен с графиком
2. ✅ Уведомления о снижении цен
3. ✅ Офлайн кэширование
4. ✅ Pull-to-refresh
5. ✅ Unit тесты

---

## 🔧 Технические детали для исправления

### 1. JWT Аутентификация

**Backend:**
```python
# Установить: pip install python-jose[cryptography]
from jose import JWTError, jwt
from datetime import datetime, timedelta

SECRET_KEY = "your-secret-key"  # Вынести в .env
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
    return encoded_jwt
```

**Android:**
```kotlin
// SharedPreferences для хранения токена
class AuthManager(context: Context) {
    private val prefs = context.getSharedPreferences("auth_prefs", Context.MODE_PRIVATE)
    
    fun saveToken(token: String) {
        prefs.edit().putString("access_token", token).apply()
    }
    
    fun getToken(): String? = prefs.getString("access_token", null)
    
    fun isLoggedIn(): Boolean = getToken() != null
}
```

### 2. Переменные окружения

**Backend:**
```python
# .env файл
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/dbname
SECRET_KEY=your-secret-key-here
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# database.py
from dotenv import load_dotenv
import os

load_dotenv()
sql_url_db = os.getenv("DATABASE_URL")
```

### 3. Конфигурация Android

**app/build.gradle.kts:**
```kotlin
android {
    buildTypes {
        debug {
            buildConfigField("String", "BASE_URL", "\"http://172.20.10.2:8000/\"")
        }
        release {
            buildConfigField("String", "BASE_URL", "\"https://api.yourdomain.com/\"")
        }
    }
}

// RetrofitClient.kt
private const val BASE_URL = BuildConfig.BASE_URL
```

---

## 📊 Статистика проекта

- **Файлов Kotlin:** 11
- **Файлов Python:** 4
- **Эндпоинтов API:** 4
- **Экранов Android:** 4
- **TODO в коде:** 3

---

## 🎯 Заключение

Проект имеет хорошую базовую структуру, но требует доработки в плане безопасности и функциональности. Основные направления для улучшения:

1. **Безопасность** - JWT, правильный CORS, переменные окружения
2. **Функциональность** - детальные страницы, избранное, настройки
3. **UX** - улучшенная обработка ошибок, валидация, анимации
4. **Архитектура** - кэширование, офлайн режим, конфигурация

Рекомендуется начать с критических проблем безопасности, затем реализовать недостающие функции согласно приоритетам.

